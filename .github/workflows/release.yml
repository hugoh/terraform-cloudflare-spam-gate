---
name: release

on: # yamllint disable-line rule:truthy
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  release:
    uses: hugoh/gh-workflows/.github/workflows/release.yml@e00bb62efa52370ace5f3d6d3e947df33c72b08f # v1.27.0
    permissions:
      contents: write
